server:
  port: 8081

spring:
  application:
    name: ravius-whatsapp-bot
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDER=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379
      database: 15 # Banco separado para isolamento dos testes
      password: "!Yuji40028922!"
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
          min-idle: 2
          max-wait: 2000ms
        shutdown-timeout: 100ms

evolution:
  api:
    url: http://localhost:8080
    api-key: test-api-key

allowed:
  test-numbers: "5511999999999"
